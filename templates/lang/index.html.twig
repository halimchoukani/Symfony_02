{% extends 'base.html.twig' %}
{% block title %}Hello LangController!{% endblock %}
{% block body %}
<div class="example-wrapper">
    <h1>Les Languages de developpement web</h1>
    <div class='row'>
        <div class='col-md-6'>
            <h2>Frontend</h2>
            <ul>
                {% for lang in langF %}
                    <li><a href = {{lang.url}} > <img src={{lang.icon}} /> {{lang.caption}} </a></li>
                {% endfor %}
            </ul>
        </div>
        <div class='col-md-6'>
            <h2>Backend</h2>
            <ul>
                {% for lang in langB %}
                    <li><a href = {{lang.url}} > <img src={{lang.icon}} /> {{lang.caption}} </a></li>
                {% endfor %}
            </ul>
        </div>
    </div>
    <h2>Conçue par : {{ name }}</h2>
    <h2>Date de creation : {{ date }}</h2>
</div>
{% endblock %}
